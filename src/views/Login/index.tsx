import { useEffect } from "react";
import style from "./style.module.scss";

function Login() {
  useEffect(() => {
  }, []);

  return (
    <div>
      <div id="canvas" className={style.canvas}>login</div>
    </div>
  );
}

export default Login;
